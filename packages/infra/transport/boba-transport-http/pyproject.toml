[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "boba-transport-http"
version = "0.0.19.dev5"
description = "Boba HTTP transport: HttpRequest + HttpTransport + auth callables (PatAuth/BasicAuth) на httpx."
requires-python = ">=3.11,<4"
dependencies = [
    "pydantic>=2.10,<2.11",
    "boba-connections==0.0.19.dev5",
    "boba-kerberos==0.0.19.dev5",
    "boba-cancellation==0.0.19.dev5",
    "boba-krb==0.0.19.dev5",
    "httpx>=0.28,<1",
]

[project.optional-dependencies]
dev = [
    "boba-stand==0.0.19.dev5",
    "pytest>=8",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["boba*"]
namespaces = true

[project.entry-points."boba.connections"]
web = "boba.transport.http.connection:MANIFEST"
