[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "boba-db-postgres"
version = "0.0.24.dev2"
description = "Boba PostgreSQL: PostgresConfig + AsyncPostgresPool — единственный async-пул системы, с kerberos-кредами на подключение."
requires-python = ">=3.11,<4"
dependencies = [
    "pydantic>=2.10,<2.11",
    "boba-catalog==0.0.24.dev2",
    "boba-connections==0.0.24.dev2",
    "boba-kerberos==0.0.24.dev2",
    "boba-cancellation==0.0.24.dev2",
    "boba-krb==0.0.24.dev2",
    "boba-toolkit==0.0.24.dev2",
    "psycopg[binary,pool]>=3.3",
]

[project.entry-points."boba.addresses"]
postgresql = "boba.db.postgres.address:PgAddresses"

[project.optional-dependencies]
arrow = [
    "boba-toolkit[arrow]==0.0.24.dev2",
    "pyarrow>=25,<26",
]
dev = [
    "boba-stand==0.0.24.dev2",
    "pytest>=8",
]

[tool.boba.extras]
"boba.db.postgres.arrow" = "arrow"

[tool.setuptools.packages.find]
where = ["src"]
include = ["boba*"]
namespaces = true

[project.entry-points."boba.catalog"]
postgres = "boba.db.postgres.snapshot:PgSnapshot"

[project.entry-points."boba.connections"]
postgres = "boba.db.postgres.manifest:MANIFEST"
