[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "boba-db-clickhouse"
version = "0.0.19.dev4"
description = "Boba ClickHouse: ClickHouseConfig + подключение payload'а по HTTP, пароль или Kerberos SPNEGO на каждый запрос."
requires-python = ">=3.11,<4"
dependencies = [
    "pydantic>=2.10,<2.11",
    "boba-toolkit==0.0.19.dev4",
    "boba-catalog==0.0.19.dev4",
    "boba-connections==0.0.19.dev4",
    "boba-kerberos==0.0.19.dev4",
    "boba-krb==0.0.19.dev4",
]

[project.optional-dependencies]
payload = [
    "clickhouse-connect[async]>=1.6,<2",
]
dev = [
    "boba-stand==0.0.19.dev4",
    "pytest>=8",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["boba*"]
namespaces = true

[project.entry-points."boba.catalog"]
clickhouse = "boba.db.clickhouse.snapshot:ChSnapshot"

[project.entry-points."boba.connections"]
clickhouse = "boba.db.clickhouse.connection:MANIFEST"
