[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "boba-catalog-service"
version = "0.0.17.dev1"
description = "Boba catalog service: хранилище каталога данных на postgres, черновики с порциями операций, публикация версий, виды и шаринг, события шины."
requires-python = ">=3.11,<4"
dependencies = [
    "boba-catalog==0.0.17.dev1",
    "boba-db-postgres==0.0.17.dev1",
    "boba-identity==0.0.17.dev1",
    "boba-messaging==0.0.17.dev1",
    "boba-cancellation==0.0.17.dev1",
    "boba-toolkit==0.0.17.dev1",
    "boba-toolrun==0.0.17.dev1",
    "pydantic>=2.10,<3",
    "psycopg[binary,pool]>=3.2,<4",
]

[project.optional-dependencies]
dev = [
    "boba-db-clickhouse==0.0.17.dev1",
    "boba-stand==0.0.17.dev1",
    "pytest>=8",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["boba*"]
namespaces = true
