[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "boba-auth"
version = "0.0.15.dev4"
description = "Boba auth: вход пользователя общий для chainlit и studio — токены, пароли, SPNEGO, креды."
requires-python = ">=3.11,<4"
dependencies = [
    "boba-identity==0.0.15.dev4",
    "boba-connections==0.0.15.dev4",
    "boba-kerberos==0.0.15.dev4",
    "boba-krb[seal]==0.0.15.dev4",
    "boba-toolkit==0.0.15.dev4",
    "pydantic>=2.10,<3",
    "pyjwt>=2.8",
]

[project.optional-dependencies]
dev = [
    "boba-ldap==0.0.15.dev4",
    "boba-config==0.0.15.dev4",
    "boba-stand==0.0.15.dev4",
    "omegaconf>=2.3,<3",
    "pytest>=8",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["boba*"]
namespaces = true
