[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "boba-agent"
version = "0.1.0"
description = "Boba agent: orchestrator + LLM contracts + tools framework + adapters (fs-workspace, messages, openai, prompt-providers)."
requires-python = ">=3.11,<4"
dependencies = ["boba-core"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["boba*"]
namespaces = true
