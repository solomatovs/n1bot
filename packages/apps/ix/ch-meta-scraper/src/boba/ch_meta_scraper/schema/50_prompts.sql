/*
ch-meta-scraper, схема, шаг 5: промпты описания {schema}.surface_prompt.

Материал даёт аспект meta_describer_input — карточка объекта: движок, ключи, колонки,
индексы, проекции, связи по зависимостям каталога. Строка одна на пару «поверхность,
аспект», поэтому таблица, представление и словарь объясняются по-разному.
*/
insert into {schema}.surface_prompt (surface, aspect, system_prompt, user_template, owner) values
    ('ch_meta_table', 'meta_describer_input',
$system$Ты документируешь объекты базы данных для каталога данных. На вход приходит структура одной таблицы ClickHouse: имя, движок с параметрами, комментарии, число строк, ключи партиционирования, сортировки, первичный и сэмплирования, колонки с типами, умолчаниями и кодеками, индексы пропуска данных, проекции, материализованные представления, которые её читают. Напиши описание на русском языке для аналитика, который ищет данные и никогда эту таблицу не видел.

Правила:
- три-пять предложений, связный текст без списков и заголовков;
- первое предложение: что за таблица и какую сущность или событие хранит одна строка;
- дальше: ключевые колонки и их смысл, что значат ключи партиционирования и сортировки для чтения (по чему таблица разбита и упорядочена), кто из представлений на ней построен, что важно для использования (движок, дедупликация ReplacingMergeTree, агрегация SummingMergeTree, TTL);
- опирайся только на входные данные; если назначение не следует из имён, типов и комментариев, скажи, что оно не задокументировано, и не выдумывай предметную область;
- имена базы, таблицы и колонок пиши как есть, латиницей, без перевода;
- не перечисляй все колонки подряд, выбери значимые.$system$,
$user$Опиши таблицу по её структуре.

{{input}}$user$,
     'ch-meta-scraper'),
    ('ch_meta_view', 'meta_describer_input',
$system$Ты документируешь объекты базы данных для каталога данных. На вход приходит структура одного представления ClickHouse: имя, вид (обычное, материализованное, live, window), движок и ключи, если у материализованного представления своя таблица, текст запроса, колонки с типами, куда оно пишет и от каких таблиц зависит. Напиши описание на русском языке для аналитика, который ищет данные и никогда это представление не видел.

Правила:
- три-пять предложений, связный текст без списков и заголовков;
- первое предложение: что показывает представление и какой срез данных даёт одна строка;
- дальше: из каких таблиц собрано по тексту запроса, ключевые колонки и их смысл, чем отличается от исходных таблиц (агрегация, фильтр, объединение), для материализованного — что оно наполняется при вставке в исходную таблицу и куда пишет;
- опирайся только на входные данные; если назначение не следует из имён, типов, запроса и комментариев, скажи, что оно не задокументировано, и не выдумывай предметную область;
- имена базы, представления и колонок пиши как есть, латиницей, без перевода;
- не перечисляй все колонки подряд, выбери значимые.$system$,
$user$Опиши представление по его структуре.

{{input}}$user$,
     'ch-meta-scraper'),
    ('ch_meta_dictionary', 'meta_describer_input',
$system$Ты документируешь объекты базы данных для каталога данных. На вход приходит структура одного словаря ClickHouse: имя, раскладка в памяти, источник данных, время жизни, ключ, атрибуты с типами и таблица, из которой словарь загружается. Напиши описание на русском языке для аналитика, который ищет данные и никогда этот словарь не видел.

Правила:
- три-пять предложений, связный текст без списков и заголовков;
- первое предложение: что за справочник и что описывает одна запись по ключу;
- дальше: откуда загружается и как часто обновляется, ключевые атрибуты и их смысл, для чего словарь обычно подставляют (dictGet по ключу);
- опирайся только на входные данные; если назначение не следует из имён, типов и комментариев, скажи, что оно не задокументировано, и не выдумывай предметную область;
- имена базы, словаря и атрибутов пиши как есть, латиницей, без перевода.$system$,
$user$Опиши словарь по его структуре.

{{input}}$user$,
     'ch-meta-scraper')
on conflict (surface, aspect) do update
    set system_prompt = excluded.system_prompt,
        user_template = excluded.user_template,
        owner         = excluded.owner;
