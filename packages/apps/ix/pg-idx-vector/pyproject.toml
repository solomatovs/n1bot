[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "boba-pg-idx-vector"
version = "0.0.21.dev1"
description = "Векторный индексатор pg_idx_emb_e5_1024: чанки аспектов объектов ix через локальные эмбеддинги."
requires-python = ">=3.11,<4"
dependencies = [
    "boba-config==0.0.21.dev1",
    "boba-pg-ix-core==0.0.21.dev1",
    "boba-llm==0.0.21.dev1",
    "psycopg[binary]>=3.2",
    "pydantic>=2.10,<2.11",
    "tokenizers>=0.20",
]

[project.scripts]
boba-pg-idx-vector = "boba.pg_idx_vector.worker:main"

[tool.setuptools.package-data]
"boba.pg_idx_vector" = ["*/*.sql", "*/*.md", "*/*.json", "*.html"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["boba*"]
namespaces = true
