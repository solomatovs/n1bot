[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "boba-ora-meta-scraper"
version = "0.0.24.dev1"
description = "Скрапер словаря Oracle в ядро ix: node, tree, edge и поверхности ora_meta_*."
requires-python = ">=3.11,<4"
dependencies = [
    "boba-db-oracle[payload]==0.0.24.dev1",
    "boba-ix-core==0.0.24.dev1",
]

[project.optional-dependencies]
dev = [
    "boba-db-postgres==0.0.24.dev1",
    "boba-stand==0.0.24.dev1",
    "pydantic>=2.10,<2.11",
    "pytest>=8",
]

[project.scripts]
boba-ora-meta-scraper = "boba.ora_meta_scraper.worker:cli"

[tool.setuptools.package-data]
"boba.ora_meta_scraper" = ["*/*.sql"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["boba*"]
namespaces = true
