"""KbDoc-tools: tool-функции, регистрируемые во фреймворке.

- `ingest.py` — `kbdoc_ingest_paths` (workspace-relative paths → KB chunks;
  секция `[tool.kb.kbdoc.ingest]`).

Операторский CLI-runner с тем же pipeline'ом — в `boba.tool.kb.cli.kbdoc.*`.
"""
