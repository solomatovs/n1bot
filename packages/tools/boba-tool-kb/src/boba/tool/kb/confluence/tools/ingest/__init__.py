"""Confluence ingest tools: HTTP → JSON → HTML → KB-коллекция.

- `space`  — все страницы перечисленных Confluence space'ов.
- `page`   — явный список page_ids.
- `cql`    — страницы, отобранные CQL-запросом.
"""
