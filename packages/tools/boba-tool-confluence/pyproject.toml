[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "boba-tool-confluence"
version = "0.0.24.dev2"
description = "Инструменты Confluence: чтение страниц и вложений, индексация их в базу знаний — всё в песочнице."
requires-python = ">=3.11,<4"
dependencies = [
    "boba-toolkit==0.0.24.dev2",
    "boba-confluence==0.0.24.dev2",
    "boba-db-postgres==0.0.24.dev2",
    "boba-db-pgvector==0.0.24.dev2",
    "boba-doc==0.0.24.dev2",
    "boba-indexing==0.0.24.dev2",
    "boba-llm==0.0.24.dev2",
    "boba-text==0.0.24.dev2",
    "boba-transport-http==0.0.24.dev2",
    "httpx[http2]>=0.28,<1",
    "pydantic>=2.10,<2.11",
]

# зависимости payload'а: нужны только внутри песочницы,
# в окружении приложения их быть не должно
[project.entry-points."boba.tools"]
confluence = "boba.tool.confluence.plugin:CONFLUENCE"
ingest = "boba.tool.confluence.plugin:INGEST"

[project.optional-dependencies]
payload = [
    "boba-doc[readers,ocr]==0.0.24.dev2",
    "boba-confluence[html]==0.0.24.dev2",
    "lxml>=5.0",
]
dev = [
    "boba-runtime==0.0.24.dev2",
    "boba-sandbox==0.0.24.dev2",
    "boba-config==0.0.24.dev2",
    "boba-confluence[html]==0.0.24.dev2",
    "boba-stand==0.0.24.dev2",
    "omegaconf>=2.3,<3",
    "psycopg[binary,pool]>=3.2",
    "pytest>=8",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["boba*"]
namespaces = true

[tool.boba.sandbox]
imports = ["fastembed", "pypdfium2", "rapidocr"]
entry_points = ["boba.llm"]
