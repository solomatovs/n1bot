# Standalone Note

This file has no metadata block — no `---` separator, no `**key:** value`
lines. The parser should treat everything after the H1 as body text and
produce a chunk with empty tags and no extra metadata.

Just a plain reference note for cases when the operator doesn't need
tags or source links.
