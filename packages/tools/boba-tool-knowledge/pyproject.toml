[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "boba-tool-knowledge"
version = "0.0.12.dev3"
description = "Документы и база знаний: чтение файлов, Confluence, индексация и поиск — всё в песочнице."
requires-python = ">=3.11,<4"
dependencies = [
    "boba-toolkit==0.0.12.dev3",
    "boba-db-postgres==0.0.12.dev3",
    "boba-db-pgvector==0.0.12.dev3",
    "boba-indexing==0.0.12.dev3",
    "boba-kbdoc==0.0.12.dev3",
    "boba-text==0.0.12.dev3",
    "boba-transport-http==0.0.12.dev3",
    "langchain-core",
    "httpx[http2]>=0.28,<1",
    "psycopg[binary,pool]>=3.2",
    "pydantic>=2.10,<2.11",
]

# зависимости payload'а: нужны только внутри песочницы,
# в окружении приложения их быть не должно
[project.optional-dependencies]
payload = [
    "lxml>=5.0",
    "boba-liteparse==0.0.12.dev3",
    "fastembed>=0.4,<1",
    "liteparse>=2,<3",
    "beautifulsoup4>=4.12",
    "markdownify>=0.11,<1.0",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["boba*"]
namespaces = true
