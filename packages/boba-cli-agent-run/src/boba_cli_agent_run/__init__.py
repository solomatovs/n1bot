"""Operator CLI: прогон одного агентского запроса.

Точка входа — :func:`boba_cli_agent_run.cli.main`, экспортируется через
``[project.scripts]`` (см. pyproject.toml) под именем
``boba-cli-agent-run``. Также доступно как
``python -m boba_cli_agent_run``.
"""
