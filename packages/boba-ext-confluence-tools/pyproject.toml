[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "boba-ext-confluence-tools"
version = "0.1.0"
description = "Boba Confluence-export navigation tools: outline + section reading via workspace files."
requires-python = ">=3.11,<4"
dependencies = [
    "boba-tools-framework",
    "boba-foundation",
    "boba-confluence-shared",
    "beautifulsoup4>=4.12",
    "lxml>=5.0",
]

[project.entry-points."boba.tools"]
confluence = "boba.ext.confluence_tools:register_tools"

[project.entry-points."boba.config_sections"]
confluence = "boba.ext.confluence_tools.config:ConfluenceSection"
confluence_outline = "boba.ext.confluence_tools.outline:ConfluenceOutlineToolSection"
confluence_section = "boba.ext.confluence_tools.section:ConfluenceSectionToolSection"

[tool.setuptools.packages.find]
where = ["src"]
include = ["boba*"]
namespaces = true
