[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "boba-tools"
version = "0.1.0"
description = "Boba tools: декларативный @tool/@provides framework на Dishka DI + Tool ABC + ToolRegistry/Catalog/Executor."
requires-python = ">=3.11,<4"
dependencies = [
    "boba-patterns",
    "boba-workspace",
    "dishka>=1.9",
    # Синхронизировано с boba-llm/boba-agent/boba-chainlit-agent: chainlit
    # 2.11+ ломает ForwardRef-резолв на pydantic 2.11+.
    "pydantic>=2.10,<2.11",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["boba*"]
namespaces = true
