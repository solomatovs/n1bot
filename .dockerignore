*

!packages
!build
!pyproject.toml
!uv.lock

packages/**/__pycache__
packages/**/*.egg-info

.venv

# в контекст не уезжают: артефакты песочницы собирают стадии Dockerfile
# (sandbox-rootfs, sandbox-site, fuse2fs-build), колёса — стадия boba-wheels
build/src/sandbox
build/src/wheels
