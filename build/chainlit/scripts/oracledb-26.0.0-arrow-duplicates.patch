diff -ru a/src/oracledb/impl/arrow/array.pyx b/src/oracledb/impl/arrow/array.pyx
--- a/src/oracledb/impl/arrow/array.pyx	2026-09-10 19:09:18.885553100 +0300
+++ b/src/oracledb/impl/arrow/array.pyx	2026-09-23 00:00:30.745623917 +0300
@@ -218,7 +218,7 @@
             init_arrow_decimal(self.schema_impl, &decimal)
             ArrowArrayViewGetDecimalUnsafe(&array.arrow_array_view, index,
                                            &decimal)
-            _check_nanoarrow(ArrowArrayAppendDecimal(array.arrow_array,
+            _check_nanoarrow(ArrowArrayAppendDecimal(self.arrow_array,
                                                      &decimal))
         elif array.schema_impl.arrow_type in (
                 NANOARROW_TYPE_BINARY,
diff -ru a/src/oracledb/impl/thin/messages/base.pyx b/src/oracledb/impl/thin/messages/base.pyx
--- a/src/oracledb/impl/thin/messages/base.pyx	2026-09-10 19:09:18.891464000 +0300
+++ b/src/oracledb/impl/thin/messages/base.pyx	2026-09-22 23:59:03.071557426 +0300
@@ -1444,9 +1444,16 @@
                 var_impl._has_returned_data = True
             elif self.cursor_impl.fetching_arrow:
                 if self._is_duplicate_data(i):
-                    var_impl._arrow_array.append_last_value(
-                        var_impl._last_arrow_array
-                    )
+                    # when the row is being reprocessed after running out of
+                    # packets, columns already appended to their arrays have
+                    # been given a temporary (empty) array; a duplicate value
+                    # for such a column was already appended on the first
+                    # pass, so nothing must be appended again (the temporary
+                    # array is empty and has no "last value" to copy)
+                    if var_impl._saved_arrow_array is None:
+                        var_impl._arrow_array.append_last_value(
+                            var_impl._last_arrow_array
+                        )
                 else:
                     self._process_column_data(buf, var_impl, self.row_index)
                 var_impl._last_arrow_array = None
